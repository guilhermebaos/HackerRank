function getArea(length, width) {
    let area = Number(length) * Number(width)
    return area;
}

function getPerimeter(length, width) {
    let perimeter = 2 * Number(length) + 2 * Number(width)
    return perimeter;
}